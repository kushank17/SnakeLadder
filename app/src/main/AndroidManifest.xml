<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kushank.chaudhary.snakeladder">

    <application
        android:allowBackup="true"
        android:icon="@drawable/sal_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/sal_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SingleActivity"/>
        <activity android:name=".MultiActivity"/>

    </application>

</manifest>